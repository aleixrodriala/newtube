<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          tools:ignore="MissingLeanbackLauncher">

    <!-- Shield on Android 11 speech recognizer not working fix? -->
    <!-- https://stackoverflow.com/questions/64319117/speechrecognizer-not-available-when-targeting-android-11 -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <permission
        android:name="${applicationId}.ACCESS_VIDEO_DATA"
        android:protectionLevel="signature" />

    <uses-permission android:name="${applicationId}.ACCESS_VIDEO_DATA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <!-- NEWTUBE(phone-only): kept as the tools:replace target for stmobile's overlay
         (which forces required=false). Value here is already false. -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <!-- android:usesCleartextTraffic - allow HTTP traffic -->
    <!-- Alt cleartext options: https://stackoverflow.com/questions/45940861/android-8-cleartext-http-traffic-not-permitted -->
    <application
        android:name="com.liskovsoft.smartyoutubetv2.tv.ui.main.MainApplication"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:allowAudioPlaybackCapture="true"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:theme="@style/Theme.NewTube"
        tools:node="merge"
        tools:replace="android:theme"
        tools:ignore="GoogleAppIndexingWarning,LockedOrientationActivity,UnusedAttribute,DiscouragedApi">

        <!-- This activity's name used inside atv channels -->
        <!-- More info: com.liskovsoft.leanbackassistant.utils.AppUtil -->
        <activity
            android:name="com.liskovsoft.smartyoutubetv2.tv.ui.main.SplashActivity"
            android:exported="true"
            android:launchMode="singleInstance"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation|layoutDirection"
            android:screenOrientation="unspecified">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- External-intent router (watch links, shares, vnd.youtube). Lives in its OWN ephemeral
             task: Android dedupes a launch whose intent filterEquals a recents task's ROOT intent
             into a plain task-to-front with NO app callback, so when these filters sat on the
             main-task Splash, re-opening the same URL (second share tap, notification) was a
             silent no-op while the app was alive - and dead task records survive force-stop, so
             it kept matching forever. A standard-launchMode activity with its own affinity always
             gets onCreate; it routes (SplashPresenter chain), finishes, and the empty
             excludeFromRecents task evaporates, leaving no record to dedupe against.
             ViewManager adds FLAG_ACTIVITY_NEW_TASK, so routed views land in the main task. -->
        <activity
            android:name="com.liskovsoft.smartyoutubetv2.tv.ui.main.IntentRouterActivity"
            android:exported="true"
            android:taskAffinity=":router"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation|layoutDirection"
            android:screenOrientation="unspecified">
            <!-- Process intents from other apps -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <action android:name="android.media.action.MEDIA_PLAY_FROM_SEARCH"/>
                <action android:name="android.nfc.action.NDEF_DISCOVERED"/>

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:scheme="http"/>
                <data android:scheme="https"/>
                <data android:host="youtube.com"/>
                <data android:host="www.youtube.com"/>
                <data android:host="m.youtube.com"/>
                <data android:host="youtu.be"/>
                <data android:pathPattern=".*"/>
                <data android:host="search" android:scheme="youtube"/>
                <data android:host="play" android:scheme="youtube"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <action android:name="android.media.action.MEDIA_PLAY_FROM_SEARCH"/>
                <action android:name="android.nfc.action.NDEF_DISCOVERED"/>

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:scheme="vnd.youtube"/>
                <data android:scheme="vnd.youtube.launch"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
        </activity>

    </application>

</manifest>
