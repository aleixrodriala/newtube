<?xml version="1.0" encoding="utf-8"?>
<!--
  A single comment (or reply) row for CommentsSheet/CommentsAdapter: circular author
  avatar on the left; author + relative time, the comment text, and a footer with a
  like affordance (icon + count) and a "View N replies" toggle. Replies reuse this same
  layout with a larger start inset applied programmatically and the replies toggle hidden.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/comment_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <ImageView
        android:id="@+id/comment_avatar"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:contentDescription="@null"
        android:src="@drawable/ic_watch_channel_placeholder" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/comment_author"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/mobile_color_on_surface_secondary"
            android:textSize="12sp"
            tools:text="\@author • 2 days ago" />

        <TextView
            android:id="@+id/comment_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:autoLink="web"
            android:textColor="@color/mobile_color_on_surface"
            android:textColorLink="@color/mobile_color_link"
            android:textSize="14sp"
            tools:text="This is a great comment that might wrap across several lines of text." />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <LinearLayout
                android:id="@+id/comment_like_group"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="?attr/selectableItemBackground"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:paddingEnd="8dp">

                <ImageView
                    android:id="@+id/comment_like_icon"
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:contentDescription="@string/mobile_watch_like"
                    android:src="@drawable/ic_watch_thumb_up" />

                <TextView
                    android:id="@+id/comment_like_count"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="4dp"
                    android:textColor="@color/mobile_color_on_surface_secondary"
                    android:textSize="12sp"
                    tools:text="1.2K" />

            </LinearLayout>

            <TextView
                android:id="@+id/comment_replies_toggle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:background="?attr/selectableItemBackground"
                android:clickable="true"
                android:focusable="true"
                android:paddingHorizontal="8dp"
                android:paddingVertical="4dp"
                android:textColor="@color/mobile_color_link"
                android:textSize="12sp"
                android:textStyle="bold"
                android:visibility="gone"
                tools:text="View 5 replies"
                tools:visibility="visible" />

        </LinearLayout>

    </LinearLayout>

</LinearLayout>
